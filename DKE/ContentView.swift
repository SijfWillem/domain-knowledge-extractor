import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("DKE - Domain Knowledge Extractor")
            .frame(minWidth: 800, minHeight: 500)
    }
}

#Preview {
    ContentView()
}
